# 
pip install torch torchvision torchaudio -i https://mirror.sjtu.edu.cn/pypi/web/simple

# 安装 Hugging Face 模型库、矩阵计算和图像处理库
pip install transformers accelerate pillow numpy

# 安装 Gymnasium (取代了老版本的 gym)
pip install gymnasium

# 安装视频生成相关的库 (带上 ffmpeg 插件才能输出 mp4)
pip install imageio imageio[ffmpeg] imageio[pyav]

# 安装 Flash Attention
# 注意：这一步需要编译 C++ 代码，可能会卡住 10 到 20 分钟，请耐心等待，切勿强行中断。
pip install flash-attn --no-build-isolation





# 检验
python -c "import torch, transformers, gymnasium, imageio; print(f'PyTorch: {torch.__version__} | CUDA Available: {torch.cuda.is_available()} | Transformers: {transformers.__version__} | Gymnasium: {gymnasium.__version__}')"